// Update with your config settings.
const settings = require('./settings.json');
module.exports = {

  development: {
    client: 'pg',
    connection: `postgres://${settings.user}:${settings.password}@${settings.hostname}:${settings.port}/${settings.database}`
  },

  staging: {
    client: 'postgresql',
    connection: {
      database: 'my_db',
      user: 'username',
      password: 'password'
    },
    pool: {
      min: 2,
      max: 10
    },
    migrations: {
      tableName: 'knex_migrations'
    }
  },

  production: {
    client: 'postgresql',
    connection: {
      database: 'my_db',
      user: 'username',
      password: 'password'
    },
    pool: {
      min: 2,
      max: 10
    },
    migrations: {
      tableName: 'knex_migrations'
    }
  }

};